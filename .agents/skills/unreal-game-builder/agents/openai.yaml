interface:
  display_name: "Unreal Game Builder"
  short_description: "Build Unreal scenes through a safe JSON harness"
  default_prompt: "Use $unreal-game-builder to place three existing Blueprint actors in the current Unreal level and save it."
policy:
  allow_implicit_invocation: true
